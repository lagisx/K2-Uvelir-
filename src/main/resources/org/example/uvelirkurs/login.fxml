<?xml version="1.0" encoding="UTF-8"?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.control.*?>

<StackPane xmlns:fx="http://javafx.com/fxml"
           fx:controller="org.example.uvelirkurs.controllers.LoginController"
           stylesheets="@styles.css"
           style="-fx-background-color: linear-gradient(to bottom right, #4a5fd8, #764ba2);">

    <VBox alignment="CENTER" spacing="25" maxWidth="420" maxHeight="550" styleClass="card">

        <VBox alignment="CENTER" spacing="10">
            <Label text="💎" style="-fx-font-size: 48px;"/>
            <Label text="Добро пожаловать" styleClass="label-title"/>
            <Label text="Войдите в свой аккаунт" styleClass="label-subtitle"/>
        </VBox>

        <VBox spacing="8">
            <Label text="Email" styleClass="label-subtitle"/>
            <TextField fx:id="emailField"
                       promptText="Введите email"
                       styleClass="text-field"
                       prefWidth="320"/>
        </VBox>

        <VBox spacing="8">
            <Label text="Пароль" styleClass="label-subtitle"/>
            <PasswordField fx:id="passwordField"
                           promptText="Введите пароль"
                           styleClass="text-field"
                           prefWidth="320"/>
        </VBox>

        <Label fx:id="errorLabel"
               visible="false"
               wrapText="true"
               maxWidth="320"
               styleClass="error-text"/>

        <Button text="Войти"
                onAction="#handleLogin"
                styleClass="button-primary"
                maxWidth="Infinity"
                prefWidth="320"/>

        <HBox alignment="CENTER" spacing="10" maxWidth="320">
            <Separator HBox.hgrow="ALWAYS" styleClass="separator-line"/>
            <Label text="или" styleClass="label-small"/>
            <Separator HBox.hgrow="ALWAYS" styleClass="separator-line"/>
        </HBox>

        <HBox alignment="CENTER" spacing="5">
            <Label text="Нет аккаунта?" styleClass="label-small"/>
            <Hyperlink text="Зарегистрироваться"
                       onAction="#goToRegister"
                       style="-fx-text-fill: #4a5fd8;"/>
        </HBox>

    </VBox>

</StackPane>
