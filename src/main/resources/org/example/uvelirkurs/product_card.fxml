<?xml version="1.0" encoding="UTF-8"?>
<?import javafx.scene.layout.VBox?>
<?import javafx.scene.layout.HBox?>
<?import javafx.scene.control.Button?>
<?import javafx.scene.control.Label?>
<?import javafx.scene.image.ImageView?>

<VBox xmlns:fx="http://javafx.com/fxml"
      fx:controller="org.example.uvelirkurs.controllers.ProductCardController"
      spacing="10"
      prefWidth="220"
      styleClass="product-card"
      stylesheets="@styles.css">

    <ImageView fx:id="imageView"
               fitWidth="196"
               fitHeight="120"
               preserveRatio="true"
               smooth="true"
               style="-fx-background-radius: 6; -fx-border-radius: 6; -fx-border-color: #e0e0e0; -fx-border-width: 1;"/>

    <Label fx:id="nameLabel"
           wrapText="true"
           styleClass="product-name"/>

    <Label fx:id="materialLabel"
           styleClass="label-small"/>

    <Label fx:id="priceLabel"
           styleClass="product-price"/>

    <HBox spacing="8">
        <Button text="Подробнее"
                styleClass="button-secondary"
                onAction="#onDetails"
                maxWidth="Infinity"
                HBox.hgrow="ALWAYS"/>
        <Button text="🛒"
                styleClass="button-primary"
                onAction="#addToCart"
                maxWidth="Infinity"
                HBox.hgrow="ALWAYS"/>
    </HBox>

</VBox>
