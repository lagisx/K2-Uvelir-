<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<?import java.lang.String?>
<VBox xmlns="http://javafx.com/javafx"
      xmlns:fx="http://javafx.com/fxml"
      fx:controller="org.example.uvelirkurs.controllers.ManagerPanelController"
      spacing="20" style="-fx-background-color: #f5f5f5;">

    <padding>
        <Insets top="20" right="20" bottom="20" left="20"/>
    </padding>

    <!-- Заголовок с кнопками -->
    <HBox alignment="CENTER_LEFT" spacing="20">
        <Button text="← Назад" onAction="#goBack" styleClass="back-button"
                style="-fx-font-size: 14px; -fx-padding: 10 20;"/>
        <VBox spacing="5">
            <Label text="Панель Менеджера / Модератора" 
                   style="-fx-font-size: 26px; -fx-font-weight: bold; -fx-text-fill: #333333;"/>
            <Label text="Управление товарами и каталогом" 
                   style="-fx-font-size: 14px; -fx-text-fill: #666666;"/>
        </VBox>
        <Region HBox.hgrow="ALWAYS"/>
        <Button text="🚪 Выйти из аккаунта" 
                onAction="#logout"
                styleClass="danger-button"
                style="-fx-font-size: 14px; -fx-padding: 10 20;"/>
    </HBox>

    <!-- Статус и индикатор загрузки -->
    <HBox alignment="CENTER" spacing="10">
        <Label fx:id="statusLabel" visible="false" style="-fx-font-size: 14px;"/>
        <ProgressIndicator fx:id="loadingIndicator" prefWidth="30" prefHeight="30" visible="false"/>
    </HBox>

    <!-- Управление товарами -->
    <VBox spacing="20" VBox.vgrow="ALWAYS" 
          style="-fx-background-color: white; -fx-padding: 25; -fx-background-radius: 12; -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.1), 10, 0, 0, 2);">
        
        <!-- Поиск и кнопка добавления -->
        <HBox spacing="15" alignment="CENTER_LEFT">
            <VBox spacing="5">
                <Label text="🔍 Поиск товаров" style="-fx-font-size: 14px; -fx-font-weight: bold; -fx-text-fill: #333333;"/>
                <HBox spacing="10">
                    <TextField fx:id="searchField" promptText="Поиск по названию, материалу..." 
                               prefWidth="400" 
                               style="-fx-font-size: 14px; -fx-padding: 10 15;"/>
                    <Button text="Найти" onAction="#searchProducts" styleClass="primary-button"
                            style="-fx-font-size: 14px;"/>
                </HBox>
            </VBox>
            <Region HBox.hgrow="ALWAYS"/>
            <Button text="➕ Добавить товар" onAction="#addProduct" 
                    styleClass="success-button" 
                    style="-fx-font-size: 16px; -fx-font-weight: bold; -fx-padding: 14 28;"/>
        </HBox>

        <Separator style="-fx-background-color: #e0e0e0;"/>

        <!-- Таблица товаров -->
        <VBox VBox.vgrow="ALWAYS" spacing="10">
            <Label text="📦 Каталог товаров" 
                   style="-fx-font-size: 18px; -fx-font-weight: bold; -fx-text-fill: #333333;"/>
            
            <TableView fx:id="productsTable" VBox.vgrow="ALWAYS"
                       style="-fx-background-radius: 8;">
                <columns>
                    <TableColumn fx:id="productIdColumn" text="ID" prefWidth="60"/>
                    <TableColumn fx:id="productNameColumn" text="Название" prefWidth="350"/>
                    <TableColumn fx:id="materialColumn" text="Материал" prefWidth="180"/>
                    <TableColumn fx:id="priceColumn" text="Цена (₽)" prefWidth="120"/>
                    <TableColumn fx:id="stockColumn" text="На складе" prefWidth="100"/>
                </columns>
                <placeholder>
                    <VBox alignment="CENTER" spacing="10" style="-fx-padding: 40;">
                        <Label text="📭" style="-fx-font-size: 48px;"/>
                        <Label text="Нет товаров в каталоге" 
                               style="-fx-font-size: 16px; -fx-text-fill: #999999;"/>
                        <Label text="Добавьте первый товар, нажав кнопку 'Добавить товар'" 
                               style="-fx-font-size: 13px; -fx-text-fill: #bbbbbb;"/>
                    </VBox>
                </placeholder>
            </TableView>
        </VBox>

        <!-- Подсказка -->
        <HBox spacing="10" alignment="CENTER_LEFT" 
              style="-fx-background-color: #e8f5e9; -fx-padding: 12 15; -fx-background-radius: 8;">
            <Label text="💡" style="-fx-font-size: 18px;"/>
            <VBox spacing="3">
                <Label text="Управление товарами:" 
                       style="-fx-font-size: 13px; -fx-font-weight: bold; -fx-text-fill: #2e7d32;"/>
                <Label text="• Нажмите правой кнопкой мыши на товаре для просмотра, редактирования, добавления изображений или удаления" 
                       wrapText="true"
                       style="-fx-font-size: 12px; -fx-text-fill: #388e3c;"/>
                <Label text="• Изображения добавляются после создания товара через контекстное меню" 
                       wrapText="true"
                       style="-fx-font-size: 12px; -fx-text-fill: #388e3c;"/>
            </VBox>
        </HBox>
    </VBox>

    <stylesheets>
        <String fx:value="@manager-panel-style.css"/>
    </stylesheets>
</VBox>
