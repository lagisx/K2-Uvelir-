<?xml version="1.0" encoding="UTF-8"?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.control.*?>

<BorderPane xmlns:fx="http://javafx.com/fxml"
            fx:controller="org.example.uvelirkurs.controllers.CartController"
            styleClass="main-background"
            stylesheets="@styles.css">

    <top>
        <HBox alignment="CENTER_LEFT" spacing="15" styleClass="top-bar">
            <Button text="← Назад"
                    onAction="#goBack"
                    styleClass="button-secondary"/>
            <Label text="Корзина" styleClass="label-title"/>
            <Region HBox.hgrow="ALWAYS"/>
            <Button text="🗑️ Очистить"
                    onAction="#clearCart"
                    styleClass="button-danger"/>
        </HBox>
    </top>

    <center>
        <ScrollPane fitToWidth="true" styleClass="scroll-pane">
            <VBox alignment="TOP_CENTER" spacing="20" style="-fx-padding: 25;">

                <VBox fx:id="cartItemsContainer"
                      spacing="12"
                      maxWidth="900"/>

                <VBox fx:id="emptyCartContainer"
                      alignment="CENTER"
                      spacing="15"
                      visible="false"
                      style="-fx-padding: 60 0;">
                    <Label text="🛒" style="-fx-font-size: 64px;"/>
                    <Label text="Корзина пуста" styleClass="empty-state"/>
                    <Label text="Добавьте товары из каталога" styleClass="label-small"/>
                </VBox>

            </VBox>
        </ScrollPane>
    </center>

    <bottom>
        <VBox spacing="0" style="-fx-background-color: white; -fx-border-color: #e0e0e0; -fx-border-width: 1 0 0 0;">
            <VBox spacing="15" style="-fx-padding: 25;">

                <HBox alignment="CENTER" spacing="15">
                    <Label text="Итого:" styleClass="label-subtitle"/>
                    <Region HBox.hgrow="ALWAYS"/>
                    <Label fx:id="totalPriceLabel"
                           text="0 ₽"
                           styleClass="total-price"/>
                </HBox>

                <HBox spacing="12" alignment="CENTER">
                    <Button text="Продолжить покупки"
                            onAction="#goBack"
                            styleClass="button-secondary"
                            maxWidth="Infinity"
                            HBox.hgrow="ALWAYS"
                            prefWidth="250"/>
                    <Button fx:id="checkoutButton"
                            text="Оформить заказ"
                            onAction="#handleCheckout"
                            styleClass="button-primary"
                            maxWidth="Infinity"
                            HBox.hgrow="ALWAYS"
                            prefWidth="250"/>
                </HBox>

            </VBox>
        </VBox>
    </bottom>

</BorderPane>
